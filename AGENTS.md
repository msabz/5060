# G1 Public Repository Agent Notice

This repository contains the public product code and public-safe documentation. It is **not** the sole canonical project-memory/control repository for project-authorized G1 ChatGPT work.

Before project execution, agents with access must read:

1. `msabz/shizuku-controller/G1_POLICY_REGISTRY.md`
2. the permanent policies named there
3. `msabz/shizuku-controller/CURRENT_CONTINUATION.md`
4. live PR/branch/HEAD/CI state in this repository

Do not bootstrap from model memory, an old public handoff, `RESUME_AFTER_ACTIONS_RESET.md`, a stale `docs/CURRENT_CONTINUATION.md` snapshot, or a PR-body embedded SHA.

If the private canonical memory is unavailable, do not infer the current G1 project state from public historical docs. Restrict work to read-only recovery/audit until canonical continuity is restored.

For source edits, preserve the G1 compliance, architecture, confidence, device and merge gates defined in the private canonical policy repository.